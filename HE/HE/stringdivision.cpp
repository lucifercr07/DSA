#include <bits/stdc++.h>

using  namespace std;

int main()
{
	int n;
	cin>>n;
	while(n--)
	{
		string s;
		cin>>s;
		int len=s.length();
		if(s.length()<=3)
			cout<<"NO"<<endl;
		else
		{
			string s1(s);
			
		}
	}
}